apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-service
  namespace: telecom
  labels:
    app: billing-service

spec:
  replicas: 1

  strategy:
    type: Recreate

  selector:
    matchLabels:
      app: billing-service

  template:
    metadata:
      labels:
        app: billing-service

    spec:
      containers:
        - name: billing-service
          image: acrtelecomossbss01.azurecr.io/billing-service:1.0.0

          ports:
            - containerPort: 8081

          envFrom:
            - configMapRef:
                name: telecom-config
            - secretRef:
                name: telecom-secret

          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 10

          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 60
            periodSeconds: 20